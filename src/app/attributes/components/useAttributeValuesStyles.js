import { makeStyles } from "@material-ui/core/styles";

export const useAttributeValuesStyles = makeStyles(
  () => ({
    tableBodyRow: {
      cursor: "pointer",
    },
  }),
  { name: "ATTRIBUTE_VALUES" }
);
